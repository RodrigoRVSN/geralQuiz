import React from "react";
import { render } from "@testing-library/react";
import "@testing-library/jest-dom/extend-expect";
import QuestionWidget from "./QuestionWidget/index";

 describe("<QuestionWidget />", () => {
   it("should true", () => {
     expect(1).toBe(1);
   });
 }); 
