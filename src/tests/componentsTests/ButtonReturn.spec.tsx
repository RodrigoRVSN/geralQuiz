import React from "react";
import { render } from "@testing-library/react";
import ButtonReturn from "../../ui/components/Buttons/ButtonReturn";

describe("<ButtonReturn />", () => {

  it("should render the button return", () => {
    render(<ButtonReturn />);
  });
});
